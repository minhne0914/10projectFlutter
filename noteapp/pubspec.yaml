name: note_app_provider
description: Notes app using Provider + ChangeNotifier (create, edit, delete with live updates)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2

flutter:
  uses-material-design: true
