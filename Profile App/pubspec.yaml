name: profile_app
description: A clean, responsive personal portfolio app (Week 1)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/avatar.png
